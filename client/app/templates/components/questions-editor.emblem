each questions as |question|
  label {{ question.title }}
  | {{input id=question.id value=question.answer type='hidden'}}
  | <trix-editor input={{ question.id }} placeholder={{ question.description }} tabindex=2></trix-editor>
