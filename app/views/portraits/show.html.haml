- content_for :title, @portrait.job_title
- content_for :og_title, "#{@portrait.first_name} partage la passion de son métier sur Bloomr : #{@portrait.job_title}"
- content_for :description, "#{@portrait.first_name} est #{@portrait.job_title} et décrit son métier "
- content_for :og_img_url, @portrait.avatar.url('thumb')
- content_for :header_background, asset_path('portrait/bg.png')

#portrait_details_2{ itemscope: '', itemtype: 'http://schema.org/QAPage' }
  = render 'header'
  = render 'links'

  = render 'my_interview'
  = render 'my_books'
  = render 'my_work'

  = render 'actions'

  .enrollment
    %p
      %span Vous aussi, vous adorez votre métier  ? Inspirez ceux qui cherchent à s'orienter !
      %a.crazy-btn{ href: enrollment_index_path } Témoignez
