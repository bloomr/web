- content_for :title, 'Les témoignages'
- content_for :description, 'Ils ont suivi le programme Bloomr et ils nous partagent leurs avis et opinions !'
- content_for :header_background, asset_path('header/header-bg.png')

#testimony
  %section.intro
    %h1 Les témoignages et avis !
    .list-wrapper
      %a{ href: bloomifesto_path }
        ←&nbsp;
        %span Le bloomifesto
      %p.subtitle Ils ont suivi le programme Bloomr et ils nous partagent leurs avis !
      %a.right{ href: press_path }
        %span La press 
        →&nbsp;

  %section
    %ul
      - Testimony.all.sort_by(&:position).each_with_index do |t, i|
        %li{ class: "testi-#{i%5}" }
          %h2 #{t.title}
          %p #{t.body}
          .legend
            = image_tag t.avatar.url('thumb'), width: 50, height: 50 unless t.avatar_file_name.blank?
            .text
              %span #{t.person} <br/> #{t.date}

